import {ViewStyle} from 'react-native';

interface CarouselCardStyles {
  container: ViewStyle;
}

const carouselCardStyles: CarouselCardStyles = {
  container: {
    marginTop: '5%'
  }
};

export default carouselCardStyles;
